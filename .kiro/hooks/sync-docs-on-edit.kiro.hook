{
  "enabled": true,
  "name": "源码变更同步文档",
  "description": "当文件写入 tool 执行后，提醒检查是否需要同步更新 docs/",
  "version": "1",
  "when": {
    "type": "postToolUse",
    "toolTypes": "fs_write|str_replace|fs_append"
  },
  "then": {
    "type": "askAgent",
    "prompt": "检查刚才写入的文件路径。仅当修改的是 src/lark_mcp_bridge/ 下的 .py 文件（新增模块、修改接口、变更行为）时，才需要检查 docs/ 是否需要同步更新。对于以下情况直接跳过，不做任何操作：修改测试文件、修改文档本身、修改配置文件、修改 .kiro/ 下的文件。"
  }
}